<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title></title>
    <style>
      body {
        margin: 0;
        font-family: Arial, Helvetica, sans-serif;
        <%if (Settings.blackTheme=="black"){%>
        background-color: gray;
        <%}%>
        <%if (Settings.blackTheme=="white"){%>
        background-color: white;
      <%}%>
      }
      .topnav {
        overflow: hidden;
        background-color: #333;
      }
      .topnav a {
        float: left;
        color: #f2f2f2;
        text-align: center;
        padding: 14px 16px;
        text-decoration: none;
        font-size: 17px;
      }
      .topnav a:hover {
        background-color: #ddd;
        color: black;
      }
      .topnav a.active {
        background-color: #04AA6D;
        color: white;
      }
      .topnav-right {
        float: right;
      }
      .footer {
        position: fixed;
        left: 0;
        width: 100%;
        padding: 40px;
        bottom: 0px;

        background-color: red;
        color: white;
        text-align: center;
      }
    </style>
  </head>
  <body>
    <div class="topnav">
      <a class="active" href="/">Home</a>
      <a href="#news">News</a>
      <a href="#contact">Contact</a>
      <% if(username==false){ %>
        <div class="topnav-right">
          <a href="/login">Login</a>
          <a href="/register">Register</a>
        </div>
      <% }%>
      <%if(username){ %>
        <div class="topnav-right">
          <a href="/console/">Console</a>
          <a href="/account/"><%=username %></a>
        </div>
      <%}%>
    </div>
    <div class="username">
      <h1><%=username%></h1>
    </div>
    <%if(!email){%>
      <form class="" action="/account/" method="post">
        <input type="email" placeholder="email" name="email">
        <input type="submit" value="Send">
      </form>
    <%}%>
    <%if(email){%>

      <div class="email">
        <h1><%=email%></h1>
      </div>
    <%}%>
    <form action="/account" method="post">
      <input type="submit" value="white" name="white">
    </form>
    <form action="/account" method="post">
      <input type="submit" value="black" name="black">
    </form>
    <div class="footer">
      <h1>ciao</h1>
    </div>
  </body>
</html>
